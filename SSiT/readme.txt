SSiTModel - сама модель
	ƒл€ инициализации модели приходит в параметры кортеж параметров скважины (Ўероховатость скажины,диаметр скважины,длина скважины)
	ƒл€ каждой скважины сво€ модель (автоматом подкачиваетс€ нужна€ моедль в зависимости от параметров)
	≈сть метод changePipe - мен€ет модель без необходимости создавать новый объект, если понадобитс€
	ƒл€ получени€ параметров всех возможных скважин есть метод getPipesList в файле controller - возвращает список кортежей параметров
	ƒл€ получени€ предикта - функци€ predict. ќна сама сделает все, что нужно с данными и выдаст предикт.
	¬ файле SSiTModel есть две переменные scalersPath,modelsPath - значени€ в них необходимо изменить, если мен€ть архитектуру. “ам задаютс€ пути папок, в которых лежат модели и скалеры

ѕример работы с модулем, обраща€сь к нему из основной директории:
from SSiT.SSiTModel import SSiTModel
from SSiT.controller import getPipesList

if __name__ == '__main__':
    model = SSiTModel(pipeChars = getPipesList()[0])
    print(model.predict(data=[[11.2736,	0.0,	199.65599999999998,	2781.8047040910365]]))
    model.changePipe(getPipesList()[1])
    print(model.predict(data=[[11.2736, 0.0, 199.65599999999998, 2781.8047040910365]]))


